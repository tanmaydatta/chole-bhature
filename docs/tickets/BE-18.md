# BE-18 · Reliability pass

> ⚠️ Detailed from the spec — refresh from the outputs of its dependencies before starting (see "Depends on").

## Context
Spec §11 (reliability/security) and §13 (risk: "eval/redeem is checkout-critical"). Depends on BE-10, BE-11, BE-12.

## Scope
- Retry/backoff tuning across BE-10/BE-11/BE-12, rate-limit tuning (BE-2), a tenant-isolation audit (every query scoped by `merchant_id`), and a standard error taxonomy/error-shape across the API.

## Acceptance criteria
- [ ] A documented error taxonomy (codes/shapes) is applied consistently across evaluate/redemptions/events.
- [ ] A tenant-isolation audit finds and fixes any query missing `merchant_id` scoping.
- [ ] Retry/backoff parameters for Queue consumers (BE-12) are tuned and documented.
- [ ] Rate-limit thresholds (BE-2) are reviewed against expected load and adjusted.
- [ ] Tests green; QA-1's suite still passes after changes.

## Technical notes
Scope-level for now; refresh the specific findings/fixes against BE-10/BE-11/BE-12's final implementations before starting — this is a pass over their stable behavior, not new endpoints.

## Interfaces
**Consumes:** BE-10/BE-11/BE-12 implementations (evaluate/redemptions/events + consumers).
**Produces:** the hardened error taxonomy and tuned retry/rate-limit parameters — consumed by QA-2/QA-3 (test against the hardened behavior), DOCS-1 (documents the final error shapes).

## Out of scope
- New endpoints or program-type behavior (hardening pass only).
- Real-merchant-traffic hardening (explicitly the next phase, Spec §2/§13).

## On completion
- [ ] Update the description of every ticket this one **Blocks** (see the Blocks property): add the concrete interfaces/decisions produced here and refresh anything stale.
