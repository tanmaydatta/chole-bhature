# BE-17 · program_stats rollups

> ⚠️ Detailed from the spec — refresh from the outputs of its dependencies before starting (see "Depends on").

## Context
Spec §5 (`program_stats` table) and §12 (Month 3 analytics-lite prep). Depends on BE-11, BE-12.

## Scope
- A queue/cron aggregation job rolling up `redemptions` (BE-11) and `wallet_transactions`/events (BE-12) into `program_stats` rows per program/merchant.

## Acceptance criteria
- [ ] The rollup job produces correct counts/sums for a seeded set of redemptions/events.
- [ ] The rollup runs on a schedule (cron) or queue trigger without double-counting on reruns.
- [ ] Tests green.

## Technical notes
Scope-level for now; refresh the exact column list against BE-11/BE-12's finalized ledger shapes before starting, in case the schema shifted since BE-1.

## Interfaces
**Consumes:** BE-11 `redemptions` ledger; BE-12 events/`wallet_transactions` data.
**Produces:** `program_stats` rows — consumed by FE-7 (analytics-lite page).

## Out of scope
- Real-time dashboards (rollup cadence only).
- Cross-merchant benchmarking.

## On completion
- [ ] Update the description of every ticket this one **Blocks** (see the Blocks property): add the concrete interfaces/decisions produced here and refresh anything stale.
