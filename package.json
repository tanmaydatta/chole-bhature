{
  "name": "incentives-platform",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "apps/identity/node_modules/.bin/oxlint scripts/staging-wrangler-config.mjs scripts/staging-wrangler-runner.mjs && pnpm -r lint",
    "verify:clean-tests": "bash scripts/verify-clean-test-gate.sh"
  }
}
