{
  "name": "@incentives/module-kit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "pnpm --filter @incentives/contracts --filter @incentives/engine build",
    "test": "vitest run",
    "lint": "oxlint src"
  },
  "dependencies": {
    "@incentives/contracts": "workspace:*",
    "@incentives/engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^4.1.9",
    "oxlint": "^1.69.0"
  }
}
