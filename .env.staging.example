# Copy to a git-ignored .env.staging, replace every placeholder, then export
# the file as documented in docs/integration/staging-operations.md.
STAGING_ENVIRONMENT=staging
STAGING_PRODUCT_D1_ID=replace-with-real-product-d1-uuid
STAGING_AUTH_D1_ID=replace-with-real-auth-d1-uuid
STAGING_OPERATOR_ORIGIN=https://replace-with-real-staging-host
STAGING_PASSKEY_RP_ID=replace-with-real-staging-host
STAGING_ALLOWED_RECIPIENTS='["replace-with-real-recipient@example.com"]'

# Required only by `pnpm --filter @incentives/identity dev:staging`.
AUTH_SECRET=replace-with-at-least-32-random-characters
RESEND_API_KEY=replace-with-staging-resend-api-key
RESEND_FROM='Identity Staging <replace-with-verified-sender@example.com>'
